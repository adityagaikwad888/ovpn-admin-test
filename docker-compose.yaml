version: '3.8'

networks:
  vpn-internal:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1372
    ipam:
      config:
        - subnet: 20.20.0.0/16

services:
  openvpn:
    image: nexus.spreezy.in/openvpn:0.1
    pull_policy: always
    command: /etc/openvpn/setup/configure.sh
    env_file:
      - ./.env
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv4.ip_forward=1
    ports:
      - 7777:1194/udp
      - 8080:8080
    volumes:
      - ./easyrsa_master:/etc/openvpn/easyrsa
      - ./ccd_master:/etc/openvpn/ccd
    dns:
      - 172.31.0.2
      - 8.8.8.8
    networks:
      - vpn-internal
    restart: always

  ovpn-admin:
    image: nexus.spreezy.in/ovpn-admin:0.1
    pull_policy: always
    command: /app/ovpn-admin
    env_file:
      - ./.env
    network_mode: service:openvpn
    volumes:
      - ./easyrsa_master:/mnt/easyrsa
      - ./ccd_master:/mnt/ccd
    depends_on:
      - openvpn
    restart: always
